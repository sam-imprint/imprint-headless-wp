import { PropsWithChildren } from 'react';
type Props = PropsWithChildren<{
    href?: string;
    tabIndex?: number;
    handleClick?: () => Promise<void>;
}> & {
    [key: string]: unknown;
};
export declare function ToolbarItem({ href, handleClick, children, ...props }: Props): JSX.Element;
export {};
