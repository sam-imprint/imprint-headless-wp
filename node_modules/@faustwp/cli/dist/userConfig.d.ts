import Configstore from 'configstore';
export type ConfigStoreType = Omit<Configstore, 'all'> & {
    all: {
        telemetry?: {
            enabled?: boolean;
            anonymousId?: string;
            notifiedAt?: number;
        };
    };
};
export declare const userConfig: ConfigStoreType;
